# @smoothstream/core

Framework-neutral Markdown analysis, stabilization, and reveal scheduling for
Smoothstream adapters. Most application users should install a renderer such as
`@smoothstream/react` or `@smoothstream/dom` instead.
