# @smoothstream/styles

Shared functional reveal CSS and default prose styling for Smoothstream web
adapters. Renderer packages install and load these styles automatically.

[Documentation](https://smoothstream.ai/docs/customize/styling)

The package also exposes `@smoothstream/styles/base.css` and
`@smoothstream/styles/styles.css` for adapter authors.
